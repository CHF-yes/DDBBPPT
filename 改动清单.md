# RGBTD 三模态项目 · 问题核实与改动清单

> 核对日期：2026-09-06
> 来源：外部审查建议（高优先级 5 条 + 中优先级 5 条）
> 结论：**10 条中 4 条真实（已修复），6 条误报（已说明，无需改动）**

---

## 一、核实结论总表

| # | 建议条目 | 核实结论 | 处理 |
|---|---|---|---|
| 1 | 预训练迁移脚本默认模型路径错误 | ❌ 误报 | 已加注释澄清 |
| 2 | RGBTD 推理时三路图像可能无法拼接 | ✅ 真实 | 已修复 |
| 3 | 训练和推理的深度处理流程不一致 | ✅ 真实 | 已修复 |
| 4 | 预训练权重迁移的层映射不正确 | ❌ 误报 | 已加防御性校验 |
| 5 | ModalConcat 权重迁移策略不成立（日志不提示） | ⚠️ 部分真实 | 已加未迁移层清单 |
| 6 | 深度逐帧 min-max 归一化破坏深度语义 | ⚠️ 部分真实 | 已优化 |
| 7 | 深度无效值处理不完整 | ✅ 真实 | 已修复 |
| 8 | 缓存文件名没有区分 RGBTD | ❌ 误报 | 无需改动 |
| 9 | 配置文件是模板，不能直接运行 | ❌ 非 bug | 无需改动 |
| 10 | 当前工作区没有 python 命令 | ❌ 环境问题 | 无需改动 |

---

## 二、真实问题修复详情

### 修复 1（对应条目 2）：三路图像 resize 尺寸不一致

**问题**：`_resize_images_3`（训练）和 loaders.py RGBTD 分支（推理）都让可见光/红外/深度**各自按自己的长边**独立缩放。若三模态原始尺寸或宽高比不同，缩放后尺寸仍不一致，`cv2.merge` 会直接报 OpenCV 错误。

**修复**：统一改为「以可见光目标尺寸为基准，把红外/深度强制 resize 到同一 (W, H)」。

- 改动文件：`ultralytics/data/base.py`（`_resize_images_3`）、`ultralytics/data/loaders.py`（RGBTD 分支）
- 深度通道继续用 `INTER_NEAREST`（最近邻），避免「0=无效」与有效深度边界产生插值伪值。

### 修复 2（对应条目 3）：训练 / 推理深度处理流程不一致

**问题**：训练路径（base.py）会做「固定深度平移对齐 + 3 通道兼容 + 有效区归一化 + 最近邻缩放」，但推理路径（loaders.py）**没有做固定深度对齐**，深度预处理也是旧的内联逻辑，导致训练看到「对齐后的深度」、推理看到「未对齐的深度」，输入分布不一致。

**修复**：给 `LoadImagesAndVideos.__init__` 增加 `depth_shift_x` / `depth_shift_y` 参数，并在 RGBTD 分支补齐与训练一致的「固定平移对齐 + 3 通道兼容 + 有效区统计归一化 + 最近邻缩放」。

- 改动文件：`ultralytics/data/loaders.py`

### 修复 3（对应条目 6 + 7）：深度归一化与无效值处理

**问题**：旧 `_preprocess_depth` 先把无效值置 0，再 `d_min = im_depth.min()`，由于无效区恒为 0，`d_min` 恒为 0，min-max 退化成「除以 max」；且有效区最小值被直接压到 0，与无效区 0 混淆。

**修复**：改为「只对有效像素（≥1e-3）统计 min/max，无效区不参与统计并强制保持 0，最后 `np.clip` 到 [0,255]」。

- 改动文件：`ultralytics/data/base.py`（`_preprocess_depth`）、`ultralytics/data/loaders.py`（RGBTD 分支同步）

### 修复 4（对应条目 5）：预训练迁移脚本日志不明确

**问题**：`transfer_pretrained.py` 只打印各分支迁移数量，没有提示哪些层保持随机初始化（如 ModalConcat 的 1×1 卷积、Detect 分类头），容易误以为「全部复用成功」。

**修复**：
1. 迁移函数记录成功迁移的 key，迁移结束后输出「保持随机初始化的层」清单（新增层/形状不匹配层）。
2. 增加尾部(head)层数一致性校验，若官方与 RGBTD 尾部层数不一致会显式告警（防御 zip 静默截断）。

- 改动文件：`transfer_pretrained.py`

---

## 三、误报条目说明（无需改动）

### 条目 1：默认模型路径「yolo11x-RGBTD-midfusion.yaml 不存在」

**实际上能正常工作**。Ultralytics 的 `yaml_model_load` 会用正则把 `yolo11x-RGBTD-midfusion.yaml` 归一化为 `yolo11-RGBTD-midfusion.yaml` 并自动应用 scale='x'（`d["scale"] = guess_model_scale(path)`）。

**注意**：若改成 `yolo11-RGBTD-midfusion.yaml`，scale 会退化为默认 'n'（模型变小），反而更糟。

已在这两处加注释澄清，避免后续误改：
- `transfer_pretrained.py`（docstring + `--dst` help）
- `train_RGBTD.py`（方式二注释）

### 条目 4：层映射 zip 静默截断

**实际不截断**。官方 YOLO11 尾部（SPPF→Detect）为 15 层，RGBTD 尾部同样 15 层（ModalConcat 在 backbone 末端、SPPF 之前，不在尾部对齐范围内），`zip` 长度一致、顺序对应正确。已加防御性长度校验（见修复 4），万一未来 head 结构改动导致长度不一致会告警。

### 条目 8：缓存文件名未区分 RGBTD

**已区分**。`base.py` 的 `generate_npy_files` 中判断条件为 `use_simotm in {"RGBT","RGBRGB6C","RGBTD"}`，RGBTD 已在集合内，会生成 `*_RGBTD.npy`。

### 条目 9：coco8-rgbtd.yaml 是模板不能直接运行

**是设计使然**。`path: /path/to/dataset` 是占位符，需用户填入实际路径；这是模板文件的标准用法，已在《RGBTD三模态模型使用教程.md》中说明。

### 条目 10：工作区没有 python 命令

**环境问题，非项目代码问题**。是本地 Windows 环境缺少 `python` 命令（只有 `python3`）。部署到 Linux 服务器后，conda/venv 激活后 `python` 均可用；若 Linux 无 `python` 可改用 `python3`。

---

## 四、改动文件清单

| 文件 | 改动类型 | 内容 |
|---|---|---|
| `ultralytics/data/base.py` | 修改 | `_preprocess_depth` 有效区统计归一化；`_resize_images_3` 以可见光为基准统一尺寸 |
| `ultralytics/data/loaders.py` | 修改 | `LoadImagesAndVideos.__init__` 增加 depth_shift 参数；RGBTD 分支补齐对齐+预处理+统一尺寸 |
| `transfer_pretrained.py` | 修改 | 记录迁移 key、输出未迁移层清单、尾部长度校验、路径机制注释 |
| `train_RGBTD.py` | 修改 | 方式二注释补充 scale 命名说明 |

---

## 五、验证

- ✅ 所有改动文件通过 `py_compile` 语法校验。
- ⚠️ 端到端运行验证需在装好 torch 的服务器上执行（本地无 torch，无法构建模型实测迁移脚本的层映射输出）。
