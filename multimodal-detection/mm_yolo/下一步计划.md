# 下一步执行计划（审计修复后 · 可照做）

> 依据：`code/runs/d0_audit/报告.md`（D0 实测）+ 本轮审计修复（`README.md` §0.1，18 项回归全过）。
> 环境：conda `EFYOLO` → `D:\Development_Tools\anaconda3\envs\EFYOLO\python.exe`
> 数据路径约定：
> - 训练：`初赛数据集-面向城市场景的多模态目标检测/train_extracted/{visible,infrared,depth}`
> - 标签：`…/训练集/new_labels_2000`（**官方修正版，15195 框**）
> - 测试：`…/test_extracted/{visible,infrared,depth}`（1000 组）

---

## 阶段 0｜修复验收（已完成）

| 项 | 命令 | 结论 |
|---|---|---|
| P0/P1 回归 | `& $py code\mm_yolo\tests\test_p0_fixes.py --root $D --labels $L` | **18/18 通过**（报告 `code/runs/p0_regression.json`） |
| 坐标往返 | `& $py code\mm_yolo\tests\test_coord_roundtrip.py --root $D --labels $L` | 往返误差 1.19e-07，提交校验 0 问题 |
| 模型自检 | `& $py code\mm_yolo\model.py` | `MM_SELFCHECK_OK`（含 EMA/重复注册/ckpt 自描述四项回归） |

**旧权重处置**：`code/runs/a0_rgb/weights/{best,last}.pt` 是"406 张黑图 + depth 双重变换 + EMA 验证绕过融合"
下训出来的，**不能作为基线、也不能提交**。目录改名保留为问题基线（`a0_rgb_broken`），新训练一律用新目录。

## 阶段 1｜重训 A0 / B0（当前）
1. **A0 锚点（RGB-only）**：从干净 COCO 权重重训 60 轮（旧目录改名为 `a0_rgb_broken` 后重新开始）。
2. **B0 三模态**：A0 结束后自动接 60 轮（看门狗），同画布 / 同轮数 / 同验证子集。
3. 判据：**同轮次 mAP 曲线**（日志里每次验证都有 `(有效类 n/12)`）+ **模态屏蔽剖面**定价。
4. 命令（无人值守，**必须脱离会话启动**）：
   ```powershell
   Start-Process powershell -ArgumentList '-ExecutionPolicy','Bypass','-File','code\mm_yolo\watchdog.ps1' -WindowStyle Hidden
   ```
   单个手动启动示例（可加 `--workers 6`）：
   ```powershell
   & $py code\mm_yolo\train.py --root $D --labels $L --name a0_rgb --modalities rgb `
       --imgsz 544x960 --batch 8 --accum 2 --workers 6 --epochs 60 --freeze-epochs 10 `
       --val-every 5 --val-limit 120 --val-conf 0.01 --save-every 1
   ```

## 阶段 2｜评测（每次训练完立即做）
5. 全量 val（400 张，12 类全覆盖，与训练同一 `split.json`）+ 逐类 AP + 条件切片：
   ```powershell
   & $py code\mm_yolo\eval.py --ckpt code\runs\b0_mm\weights\best.pt --root $D --labels $L --profile `
       --out code\runs\b0_mm\eval_full.json
   ```
6. **模态屏蔽剖面**（同一条命令的 `--profile`）：分别屏蔽 IR/depth/RGB → 给每个模态定价。
7. 提交（正式测试集，自动按 ckpt 记录的模态/画布，逐图产出且严格校验）：
   ```powershell
   & $py code\mm_yolo\submit.py --ckpt code\runs\b0_mm\weights\best.pt `
       --root "初赛数据集-面向城市场景的多模态目标检测\test_extracted" `
       --out code\runs\submit_b0 --zip
   ```

## 阶段 3｜消融（每天 2–4 个配置，全部 60 轮同口径）
8. `--no-quality` / `--no-prior` / `--no-deformable` / `--no-dropout`：四个显式注入项各值多少。
9. `--fusion-tier L0 --no-deformable` vs 默认 L2：交互值不值得。
10. `--share-tier a|b|c`：浅层共享 vs 深层分化；`--late-bus` 开关。
11. 与官方 baseline 同口径对比。

## 阶段 4｜正式化（需租 24GB 卡）
12. 300 轮、画布 720×1280、**2 种子**；推理侧多尺度 TTA；`submit.py` 出正式提交。
13. 技术报告素材：D0 报告 + 修复对照表（README §0.1）+ 剖面表 + 逐类 AP + 消融表。

---

## 已知仍要做的事（本轮未做，按优先级）
- **mosaic / copy-paste 增强**（`cfg.mosaic_p` 已定义但未接；长尾类 1/7/11 合计仅 1.7%）。
- **类别重采样**：稀有类 copy-paste 是长尾最直接的收益点。
- 训练期验证仍用 120 张子集（覆盖 12 类但稀有类框数少，选模方差未消除）
  → 正式报数一律用 `eval.py` 全量 val；子集只用于"每 5 轮看趋势"。
- `align.py`（逐图估计）已按 D0 结论不接入训练路径，仅保留为诊断工具。

## 被 D0 砍掉/降级的事项（不要再做）
- ❌ 逐图自标定 / 亚像素 warp / 全局对齐参数模型（IR 已对齐，depth 无可信偏移）
- ❌ 框预算与逐类配额（0 张 >100 框）
- ❌ 复杂残影可靠性掩码（中位 0.77%）→ 只用无效掩码
- ⬇️ SAHI 切片推理（目标中位 77px，<32² 仅 9.9%）→ 先做 960/1280 + TTA
